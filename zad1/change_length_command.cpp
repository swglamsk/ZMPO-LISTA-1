#include "change_length_command.h"
#include <iostream>
#include <string>

change_length_command::change_length_command()
{
}


change_length_command::~change_length_command()
{
}

void change_length_command::run_command()
{
	/*int index_of;
	int type_table_length;
	std::cout << "Podaj indeks obiektu ktory chcesz zmienic" << std::endl;
	std::cin >> index_of;
	if (index_of > class_temp::number_of_tables - 1 || index_of < 0)
		std::cout << "Indeks poza zasiêgiem" << std::endl;
	else
	{
		std::cout << "Podaj dlugosc tablicy" << std::endl;
		std::cin >> type_table_length;
		if (type_table_length < 0)
			std::cout << "Dlugosc nie moze byc mniejsza od 0" << std::endl;
		else
			class_temp::array_of_tables.at(index_of).set_Table_Length(type_table_length);
	}
	*/
}
